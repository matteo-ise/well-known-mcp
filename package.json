{
  "name": "well-known-mcp-monorepo",
  "version": "1.0.0",
  "description": "Reference implementation for .well-known/mcp.json",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces"
  }
}
